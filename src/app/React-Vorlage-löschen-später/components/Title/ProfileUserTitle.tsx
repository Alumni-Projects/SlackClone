import styled from 'styled-components';

const ProfileUserTitle = styled.h3`
  font-size: var(--font-size-title-default);
  font-weight: var(--font-weight-extra-bold);
  color: var(--black-color);
  margin-bottom: 15px;
`;

export default ProfileUserTitle;
