import { FormatMessageDatePipe } from './format-message-date.pipe';

describe('FormatMessageDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatMessageDatePipe();
    expect(pipe).toBeTruthy();
  });
});
