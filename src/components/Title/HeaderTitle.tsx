import styled from 'styled-components';

const HeaderTitle = styled.p`
  font-size: var(--font-weight-extra-bold);
  font-weight: var(--font-weight-extra-bold);
  color: var(--black-color);
`;

export default HeaderTitle;
